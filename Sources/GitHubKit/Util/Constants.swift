import Foundation

public enum Constants {
    
    public enum URLs {
        public static let REST_URL: URL = URL(string: "https://api.github.com")!
        public static let GraphQL_URL: URL = URL(string: "https://api.github.com/graphql")!
    }
}
